<% if flash.any? %>
<% flash.each do |key, value| %>
<div class="alert alert-<%= key %> fade in flash-messages">
<button class="close" data-dismiss="alert">
×
</button>
<span><%= value %></span>
</div>
<% end %>
<% end %>